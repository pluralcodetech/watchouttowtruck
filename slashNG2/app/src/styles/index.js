import React from 'react';
import {StyleSheet} from 'react-native';

const INPUT_STYLE = {
  body: {
    marginTop: 10,
    marginBottom: 10,
  },
};

export default STYLES = {
  input: INPUT_STYLE,
};
